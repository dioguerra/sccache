apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sccache.fullname" . }}-test-connection"
  labels:
    {{- include "sccache.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sccache.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
